apiVersion: v1
kind: Service
metadata:
  name: crawl4ai-svc
  namespace: spencer-os
spec:
  selector:
    app: crawl4ai
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
    - name: mcp
      protocol: TCP
      port: 11235
      targetPort: 11235
