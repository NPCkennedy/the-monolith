apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Spencer OS — minimal validation stack
# Synced by ArgoCD from homelab-gitops/apps/spencer-os/
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml

commonLabels:
  app.kubernetes.io/part-of: spencer-os
